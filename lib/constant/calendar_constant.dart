// ignore_for_file: non_constant_identifier_names, constant_identifier_names

import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

const Color TRANSPARENT_COLOR = Colors.transparent;
const Color BOXSHADOW_COLOR = Color.fromARGB(10, 0, 0, 0);
const Color SELECTED_DATE_COLOR = Color(0XFF222222);

const Offset BOXSHADOW_OFFSET = Offset(10.0, 10.0);

const Duration MONTH_DURATION = Duration(milliseconds: 200);
const Duration WEEK_DURATION = Duration(milliseconds: 250);

const Duration FORMAT_MONTH_DURATION = Duration(milliseconds: 250);
const Duration FORMAT_WEEK_DURATION = Duration(milliseconds: 240);

//FONT SIZE
double DATE_FONTSIZE = 13.sp;
